'use strict'

const expect = require('chai').expect

describe('index module', () => {
    it('should export a function', () => {
        // No valid commands
        const commands = ['node-path','index-path'];
      //  console.log(process.stdout);
        expect(1).to.eq(1);
    });
});